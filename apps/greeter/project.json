{
  "root": "apps/greeter",
  "sourceRoot": "apps/greeter",
  "projectType": "application",
  "targets": {
    "build": {
      "executor": "nx:run-commands",
      "options": {
        "command": "go build -o greeter.exe .",
        "cwd": "apps/greeter"
      }
    },
    "serve": {
      "executor": "nx:run-commands",
      "options": {
        "command": "go run .",
        "cwd": "apps/greeter"
      }
    }
  }
}
